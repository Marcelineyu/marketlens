import Papa from 'papaparse';import { Row } from '../types';
const formattedNumber=(value:unknown)=>{if(typeof value!=='string')return value;const text=value.trim();if(!text)return value;const percent=text.match(/^([+-]?(?:\d{1,3}(?:,\d{3})*|\d+)(?:\.\d+)?)%$/);if(percent)return Number(percent[1].replace(/,/g,''))/100;const amount=text.match(/^([+-]?)\$?((?:\d{1,3}(?:,\d{3})+|\d+)(?:\.\d+)?)$/);return amount?Number(`${amount[1]}${amount[2].replace(/,/g,'')}`):value};
const normalizeNumbers=(rows:Row[])=>rows.map(row=>Object.fromEntries(Object.entries(row).map(([key,value])=>[key,formattedNumber(value)])));
export async function parseFile(file:File):Promise<Row[]>{if(file.size>10*1024*1024)throw new Error('File is larger than 10 MB.');const ext=file.name.split('.').pop()?.toLowerCase();if(!['csv','xlsx','xls'].includes(ext||''))throw new Error('Choose a CSV, XLSX, or XLS file.');let rows:Row[]=[];if(ext==='csv'){const text=await file.text();if(!text.trim())throw new Error('This file is empty.');const r=Papa.parse<Row>(text,{header:true,dynamicTyping:true,skipEmptyLines:'greedy'});if(r.errors.length)throw new Error('We could not parse this CSV because some rows have an inconsistent structure.');rows=normalizeNumbers(r.data)}else{const XLSX=await import('xlsx');const wb=XLSX.read(await file.arrayBuffer());rows=normalizeNumbers(XLSX.utils.sheet_to_json<Row>(wb.Sheets[wb.SheetNames[0]],{defval:null}))}if(!rows.length)throw new Error('This file has no data rows.');return rows}
